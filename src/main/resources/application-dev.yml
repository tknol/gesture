spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tnk_gesture_dev
    username: gesture_dev_user
    password: password123
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: database-create-script.sql